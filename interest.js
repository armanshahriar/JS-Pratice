const principal = 1000; // principal amount
const interestRate = 5; // interest rate (in percentage)
const time = 2; // time (in years)

const interest = (principal * interestRate * time) / 100;

console.log("Interest Amount:", interest);